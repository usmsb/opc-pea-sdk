# Mirror Notes

This example README is mirrored from `peas/dipaitang/README.md` in
`usmsb/opc-platform`.

Only documentation is mirrored here. The complete deployable PEA instance,
secrets, tests and business-specific tools stay in the OPC platform repository.
